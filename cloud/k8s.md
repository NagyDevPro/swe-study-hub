# CNCF (Cloud Native Computing Foundation) : is an organization for developemnt and managing famous cloud platforms


* Kubernates worker nodes can use multi **Container runtime** by making a general interface `CRI` "Container Runtime Interface"

* Kubernates in the past depended on `Docker` on running pods then it's now depended on `Containerd` as it's container runtime